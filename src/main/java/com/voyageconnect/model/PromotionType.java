package com.voyageconnect.model;

/**
 * Énumération des types de promotion
 */
public enum PromotionType {
    FLIGHT,     // Promotion sur les vols
    HOTEL,      // Promotion sur les hôtels
    CIRCUIT,    // Promotion sur les circuits
    ALL         // Promotion générale
}
